Git clear history

git checkout --orphan latest_branch
git add -A
git commit -am "commit message"
git branch -D master
git branch -m master
git push origin master -f

cd submodule_name
git checkout master && git pull --rebase
cd ..
git add submodule_name
git commit -m "updating submodule to latest"

git fetch origin pull/276/head:Ian2020

K8 utility

git config core.fileMode false

curl -Lo .git/hooks/commit-msg https://gerrit.ericsson.se/tools/hooks/commit-msg

chmod 766 .git/hooks/commit-msg
